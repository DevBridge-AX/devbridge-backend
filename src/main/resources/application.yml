spring:
  application:
    name: backend
  datasource:
    url: jdbc:mysql://${SPRING_DATASOURCE_HOST:localhost}:${SPRING_DATASOURCE_PORT:3306}/${SPRING_DATASOURCE_DATABASE:devbridge}?serverTimezone=Asia/Seoul&characterEncoding=UTF-8
    username: ${SPRING_DATASOURCE_USERNAME:ssafy}
    password: ${SPRING_DATASOURCE_PASSWORD:1234}
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update      # 엔티티 변경 시 DB 테이블 자동 업데이트
    show-sql: true          # 실행되는 SQL 쿼리를 터미널에 보여주기
    properties:
      hibernate:
        format_sql: true

  data:
    redis:
      host: ${SPRING_DATA_REDIS_HOST:localhost}
      port: ${SPRING_DATA_REDIS_PORT:6379}
  mail:
    host: smtp.gmail.com
    port: 587
    username: choie000208@gmail.com
    password: syvlkihnrlixjmjl
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true
            connectiontimeout: 5000
            timeout: 5000
            writetimeout: 5000

jwt:
  secret: c2VjcmV0LWtleS1mb3ItZGV2YnJpZGdlLWFpLXByb2plY3QtYXV0aGVudGljYXRpb24=
